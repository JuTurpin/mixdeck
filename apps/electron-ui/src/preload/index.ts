// Placeholder — rien d'exposé au renderer pour l'instant. La vraie surface de
// l'API (contextBridge) sera posée en Story 2.4 (Controllers) / 2.5
// (Communication UI -> Engine), en s'appuyant sur le contrat Engine API
// défini en Story 2.1 (ADR-013).
